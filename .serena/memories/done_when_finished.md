When finishing a task: verify modified files compile at minimum with `python -m py_compile ...`. Run targeted `ruff check` when practical. For runtime-sensitive changes, prefer a live smoke script against real public endpoints instead of synthetic tests. If touching reports/telemetry/persistence, inspect generated artifacts under `data/bot/session/reports` and `data/bot/telemetry`. State clearly what was verified versus what remains unverified, especially any end-to-end Binance runtime behavior.