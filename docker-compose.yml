version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config.toml:/app/config.toml:ro
    environment:
      - PYTHONUNBUFFERED=1
    command: python main.py --mode dry-run --config config.toml
